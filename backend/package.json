{
  "name": "@forkcast/backend",
  "version": "1.0.0",
  "private": true,
  "license": "ISC",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "node --watch src/index.ts",
    "start": "node src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "format": "oxfmt --config ../.oxfmt.json src",
    "lint": "oxlint --config ../oxlint.json src",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.14",
    "hono": "^4.12.14"
  }
}
